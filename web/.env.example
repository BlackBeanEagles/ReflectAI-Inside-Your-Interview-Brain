# Base URL of the FastAPI backend. Must be publicly reachable from the
# browser (this is a client-side env var -- NEXT_PUBLIC_ prefix required).
NEXT_PUBLIC_API_BASE=https://reflectinterview-api.onrender.com
